# 프로젝트 구조 V2 (완전 분리 아키텍처)

## 🎯 설계 철학 변경

### 이전 (V1): Shared 모듈 방식
```
❌ 문제점:
- GraphQL 코드를 프로덕션 베이스로 사용하기 어려움
- 테스트용 코드와 실제 사용 코드가 섞임
- REST 의존성이 GraphQL에 영향
```

### 현재 (V2): 완전 분리 방식
```
✅ 장점:
- GraphQL 디렉토리를 그대로 프로덕션 베이스 코드로 사용 가능
- REST와 GraphQL 완전 독립 (의존성 없음)
- 각자 최적의 컨벤션과 패턴 적용
- 코드 중복은 있지만 명확함
```

## 📁 새로운 프로젝트 구조

```
graph-rest-preform/
│
├── rest/                           # REST 아키텍처 (비교용)
│   ├── services/
│   │   ├── user-service/
│   │   │   ├── src/
│   │   │   │   ├── main.py
│   │   │   │   ├── models.py
│   │   │   │   ├── data.py
│   │   │   │   └── config.py
│   │   │   ├── tests/
│   │   │   ├── Dockerfile
│   │   │   └── requirements.txt
│   │   │
│   │   ├── robot-service/
│   │   │   ├── src/
│   │   │   │   ├── main.py
│   │   │   │   ├── models.py
│   │   │   │   ├── data.py
│   │   │   │   └── config.py
│   │   │   ├── tests/
│   │   │   ├── Dockerfile
│   │   │   └── requirements.txt
│   │   │
│   │   └── site-service/
│   │       ├── src/
│   │       ├── tests/
│   │       ├── Dockerfile
│   │       └── requirements.txt
│   │
│   ├── gateway/
│   │   └── nginx.conf
│   │
│   └── docker-compose.yml          # REST 전용 docker-compose
│
├── graphql/                        # GraphQL 아키텍처 (프로덕션 베이스)
│   ├── services/
│   │   ├── user-service/
│   │   │   ├── src/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── main.py
│   │   │   │   ├── schema/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── types.py        # GraphQL Types
│   │   │   │   │   ├── queries.py      # Query resolvers
│   │   │   │   │   └── mutations.py    # Mutation resolvers (미래 확장)
│   │   │   │   ├── models/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   └── user.py         # Pydantic models
│   │   │   │   ├── data/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   └── repository.py   # Data layer
│   │   │   │   ├── core/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── config.py       # Settings
│   │   │   │   │   └── logging.py      # Logging setup
│   │   │   │   └── utils/
│   │   │   │       └── __init__.py
│   │   │   ├── tests/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── test_schema.py
│   │   │   │   └── test_data.py
│   │   │   ├── Dockerfile
│   │   │   ├── requirements.txt
│   │   │   └── pyproject.toml          # Modern Python packaging
│   │   │
│   │   ├── robot-service/
│   │   │   ├── src/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── main.py
│   │   │   │   ├── schema/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── types.py
│   │   │   │   │   ├── queries.py
│   │   │   │   │   └── dataloaders.py  # DataLoader (N+1 해결)
│   │   │   │   ├── models/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── robot.py
│   │   │   │   │   └── telemetry.py
│   │   │   │   ├── data/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── robot_repository.py
│   │   │   │   │   └── telemetry_repository.py
│   │   │   │   ├── core/
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── config.py
│   │   │   │   │   └── logging.py
│   │   │   │   └── utils/
│   │   │   ├── tests/
│   │   │   ├── Dockerfile
│   │   │   ├── requirements.txt
│   │   │   └── pyproject.toml
│   │   │
│   │   └── site-service/
│   │       ├── src/
│   │       │   ├── __init__.py
│   │       │   ├── main.py
│   │       │   ├── schema/
│   │       │   ├── models/
│   │       │   ├── data/
│   │       │   ├── core/
│   │       │   └── utils/
│   │       ├── tests/
│   │       ├── Dockerfile
│   │       ├── requirements.txt
│   │       └── pyproject.toml
│   │
│   ├── gateway/
│   │   ├── router.yaml             # Apollo Router 설정
│   │   └── supergraph.graphql      # Federation schema
│   │
│   └── docker-compose.yml          # GraphQL 전용 docker-compose
│
├── monitoring/                     # 공통 모니터링 (둘 다 사용)
│   ├── prometheus/
│   │   └── prometheus.yml
│   ├── grafana/
│   │   ├── dashboards/
│   │   └── provisioning/
│   └── docker-compose.monitoring.yml
│
├── load-test/                      # 공통 부하 테스트
│   ├── locustfile_rest.py
│   ├── locustfile_graphql.py
│   └── docker-compose.loadtest.yml
│
├── scripts/                        # 유틸리티 스크립트
│   ├── start-rest.sh
│   ├── start-graphql.sh
│   ├── start-monitoring.sh
│   └── run-test.sh
│
├── docs/                           # 문서
│   ├── conversation.md
│   └── cursor/
│       ├── 프로젝트_목표_정리.md
│       ├── API-설계-상세.md
│       └── 프로젝트_구조_v2.md (이 파일)
│
├── docker-compose.full.yml         # 전체 통합 (REST + GraphQL + 모니터링)
├── .gitignore
├── .env.example
└── README.md
```

## 🏗️ 각 부분 상세 설명

### 1. REST 디렉토리 구조

**특징:**
- 간단하고 직관적
- FastAPI 기본 구조
- 테스트 비교용이므로 최소한의 구조

**각 서비스 구조:**
```
user-service/
├── src/
│   ├── main.py           # FastAPI app
│   ├── models.py         # Pydantic models
│   ├── data.py           # In-memory data + latency
│   └── config.py         # Environment config
├── tests/
│   └── test_api.py
├── Dockerfile
└── requirements.txt
```

### 2. GraphQL 디렉토리 구조 (프로덕션 수준)

**특징:**
- 클린 아키텍처 적용
- 레이어 분리 (Presentation → Business → Data)
- 확장 가능한 구조
- 프로덕션에서 바로 사용 가능

**각 서비스 구조 (상세):**
```
robot-service/
├── src/
│   ├── __init__.py
│   │
│   ├── main.py                      # FastAPI + Strawberry 진입점
│   │
│   ├── schema/                      # Presentation Layer (GraphQL)
│   │   ├── __init__.py
│   │   ├── types.py                 # GraphQL Types
│   │   │   - RobotType
│   │   │   - TelemetryType
│   │   │   - RobotWithRelationsType
│   │   ├── queries.py               # Query Resolvers
│   │   │   - robot(id: ID!)
│   │   │   - robotsBySite(siteId: ID!)
│   │   ├── mutations.py             # Mutation Resolvers (미래)
│   │   │   - updateRobotStatus(...)
│   │   └── dataloaders.py           # DataLoader (N+1 해결)
│   │       - UserLoader
│   │       - SiteLoader
│   │       - TelemetryLoader
│   │
│   ├── models/                      # Domain Models (Pydantic)
│   │   ├── __init__.py
│   │   ├── robot.py
│   │   │   - RobotModel
│   │   │   - RobotCreateModel
│   │   │   - RobotUpdateModel
│   │   └── telemetry.py
│   │       - TelemetryModel
│   │
│   ├── data/                        # Data Layer (Repository Pattern)
│   │   ├── __init__.py
│   │   ├── robot_repository.py
│   │   │   - RobotRepository
│   │   │   - get_by_id()
│   │   │   - get_by_site()
│   │   │   - get_batch()
│   │   └── telemetry_repository.py
│   │       - TelemetryRepository
│   │       - get_by_robot_id()
│   │       - get_batch()
│   │
│   ├── core/                        # Core Utilities
│   │   ├── __init__.py
│   │   ├── config.py                # Settings (pydantic-settings)
│   │   │   - Settings class
│   │   │   - get_settings()
│   │   └── logging.py               # Structured logging
│   │       - setup_logger()
│   │
│   └── utils/                       # Helper functions
│       ├── __init__.py
│       └── context.py               # GraphQL context
│           - get_context()
│
├── tests/
│   ├── __init__.py
│   ├── conftest.py                  # Pytest fixtures
│   ├── test_schema.py               # GraphQL schema tests
│   ├── test_resolvers.py            # Resolver tests
│   └── test_dataloaders.py          # DataLoader tests
│
├── Dockerfile
├── requirements.txt
├── pyproject.toml                   # Modern packaging
└── .dockerignore
```

## 🎨 GraphQL 서비스 컨벤션

### 파일 명명 규칙
```python
# 모듈
user_service/              # 소문자 + 언더스코어
robot_repository.py        # 소문자 + 언더스코어

# 클래스
class UserType:            # PascalCase
class RobotRepository:     # PascalCase

# 함수
def get_by_id():          # 소문자 + 언더스코어
async def fetch_users():  # 소문자 + 언더스코어

# 상수
MAX_BATCH_SIZE = 100      # 대문자 + 언더스코어
DEFAULT_TIMEOUT = 30
```

### Import 순서
```python
# 1. Standard library
import asyncio
from typing import List, Optional

# 2. Third-party
import strawberry
from fastapi import FastAPI

# 3. Local
from models.robot import RobotModel
from data.robot_repository import RobotRepository
from core.config import get_settings
```

### 레이어 의존성 규칙
```
schema/ (Presentation)
   ↓ 의존
models/ (Domain)
   ↓ 의존
data/ (Data Access)
   ↓ 의존
외부 시스템 (DB, API 등)

❌ data/ → schema/ 의존 금지
❌ models/ → schema/ 의존 금지
✅ schema/ → models/ → data/ 순서 준수
```

## 🐳 Docker Compose 구조

### 1. REST 전용
```yaml
# rest/docker-compose.yml
services:
  nginx:
  user-service-rest:
  robot-service-rest:
  site-service-rest:
```

### 2. GraphQL 전용
```yaml
# graphql/docker-compose.yml
services:
  apollo-router:
  user-service-graphql:
  robot-service-graphql:
  site-service-graphql:
```

### 3. 모니터링
```yaml
# monitoring/docker-compose.monitoring.yml
services:
  prometheus:
  grafana:
  cadvisor:
```

### 4. 부하 테스트
```yaml
# load-test/docker-compose.loadtest.yml
services:
  locust-master:
  locust-worker:
```

### 5. 통합 실행
```yaml
# docker-compose.full.yml
# REST + GraphQL + 모니터링 + 부하테스트 모두 포함
```

## 🚀 실행 시나리오

### Scenario 1: REST만 테스트
```bash
cd rest
docker-compose up -d
```

### Scenario 2: GraphQL만 테스트
```bash
cd graphql
docker-compose up -d
```

### Scenario 3: REST vs GraphQL 비교
```bash
# 루트에서
docker-compose -f docker-compose.full.yml up -d
```

### Scenario 4: GraphQL 개발
```bash
cd graphql/services/robot-service
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn src.main:app --reload
```

## ✅ 이 구조의 장점

### 1. 완전한 독립성
- REST와 GraphQL이 서로 영향 없음
- GraphQL 디렉토리를 그대로 다른 프로젝트로 이동 가능
- 의존성 충돌 없음

### 2. 프로덕션 준비
- GraphQL 서비스가 클린 아키텍처 기반
- 테스트 가능한 구조
- 확장 가능한 설계

### 3. 명확한 비교
- REST vs GraphQL 차이가 디렉토리로 명확히 구분
- 각자 최적의 패턴 적용
- 벤치마크 결과 해석 용이

### 4. 유지보수성
- 각 서비스가 독립적
- 레이어별 책임 명확
- 코드 찾기 쉬움

## 📊 예상 코드 중복

```
공통 데이터:
- User 100명 데이터 → REST/GraphQL 각각 보유
- Robot 200대 데이터 → REST/GraphQL 각각 보유
- Site 5개 데이터 → REST/GraphQL 각각 보유

예상 중복:
- 데이터: ~1KB × 2 = 2KB
- 로직: 각 서비스 100-200줄 × 3 × 2 = ~1200줄
- 설정: 각 파일 반복

💡 중복의 가치:
- 완전한 독립성
- GraphQL을 프로덕션 베이스로 사용 가능
- 명확한 비교
```

## 🎯 다음 단계

1. ✅ 프로젝트 구조 확정
2. GraphQL 서비스부터 프로덕션 수준으로 구현
3. REST 서비스 구현 (간단하게)
4. 모니터링 설정
5. 부하 테스트 스크립트
6. 통합 테스트 및 비교

이 구조로 GraphQL을 실제 프로젝트 베이스로 사용할 수 있습니다! 🚀
